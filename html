<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Diseño Gráfico Dinámico</title>
  <style>
    /* Modern reset and box-sizing */
    *, *::before, *::after {
      box-sizing: border-box;
    }
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: #f0f4ff;
      color: #002080;
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      overflow-x: hidden;
    }

    /* Root colors and variables */
    :root {
      --blue-base: #0000ff;
      --blue-dark: #002080;
      --blue-light: #cce0ff;
      --blue-gradient: linear-gradient(135deg, #0000ff 0%, #3399ff 100%);
      --spacing: 24px;
      --border-radius: 12px;
      --transition: 0.3s ease;
    }

    /* Container to center content and max width */
    .container {
      max-width: 1200px;
      padding: 0 24px;
      margin: 0 auto;
    }

    /* Header */
    header {
      position: sticky;
      top: 0;
      background: #001f7f;
      color: white;
      padding: 16px 24px;
      box-shadow: 0 2px 8px rgba(0,0,128,0.15);
      z-index: 1000;
      display: flex;
      justify-content: space-between;
      align-items: center;
      user-select: none;
    }
    header .logo {
      font-weight: 900;
      font-size: 1.8rem;
      background: var(--blue-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      cursor: default;
      letter-spacing: 2px;
    }

    nav {
      display: flex;
      gap: 32px;
    }
    nav a {
      color: white;
      text-decoration: none;
      font-weight: 600;
      font-size: 1rem;
      position: relative;
      padding: 8px 0;
      transition: color var(--transition);
      border-bottom: 2px solid transparent;
    }
    nav a:hover,
    nav a:focus {
      color: #99c2ff;
      border-bottom-color: #99c2ff;
      outline: none;
    }

    /* Hero section with dynamic gradient background */
    .hero {
      min-height: 75vh;
      background: linear-gradient(-45deg, #0000ff, #3399ff, #0055ff, #66a3ff);
      background-size: 400% 400%;
      animation: gradientShift 15s ease infinite;
      color: white;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 48px 24px;
      border-radius: 0 0 var(--border-radius) var(--border-radius);
      box-shadow: inset 0 0 40px rgba(0,0,100,0.5);
    }

    @keyframes gradientShift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .hero h1 {
      font-size: clamp(2.5rem, 5vw, 4rem);
      font-weight: 900;
      margin-bottom: 16px;
      text-shadow: 0 3px 8px rgba(0,0,0,0.4);
    }
    .hero p {
      font-size: clamp(1rem, 2vw, 1.25rem);
      max-width: 600px;
      margin-bottom: 32px;
      text-shadow: 0 2px 6px rgba(0,0,0,0.3);
    }
    .btn-primary {
      background: transparent;
      padding: 16px 48px;
      font-size: 1.25rem;
      font-weight: 700;
      border-radius: var(--border-radius);
      cursor: pointer;
      color: var(--blue-base);
      position: relative;
      overflow: hidden;
      border: 3px solid transparent;
      background-image: linear-gradient(#fff, #fff), var(--blue-gradient);
      background-origin: border-box;
      background-clip: padding-box, border-box;
      transition: color 0.3s ease;
      box-shadow: 0 6px 12px rgba(0,0,255,0.3);
      user-select: none;
    }
    .btn-primary:hover,
    .btn-primary:focus {
      color: white;
      background-image: none;
      background-color: var(--blue-base);
      box-shadow: 0 8px 16px rgba(0,0,255,0.5);
      outline: none;
      transform: scale(1.05);
      transition: transform 0.25s ease;
    }

    /* Features grid */
    .features {
      margin: 72px 0;
      display: grid;
      gap: 32px;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    }
    .feature-card {
      background: white;
      border-radius: var(--border-radius);
      padding: 32px 24px;
      box-shadow: 0 8px 20px rgba(0,0,255,0.1);
      transition: box-shadow var(--transition), transform var(--transition);
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
    .feature-card:hover,
    .feature-card:focus-within {
      box-shadow: 0 12px 28px rgba(0,0,255,0.3);
      transform: translateY(-8px);
      outline: none;
    }
    .feature-icon {
      font-size: 3rem;
      color: var(--blue-base);
      margin-bottom: 16px;
    }
    .feature-title {
      font-weight: 700;
      font-size: 1.25rem;
      margin-bottom: 8px;
      color: var(--blue-dark);
    }
    .feature-desc {
      font-size: 1rem;
      color: #333;
      line-height: 1.4;
    }

    /* Footer */
    footer {
      background: var(--blue-dark);
      color: white;
      padding: 48px 24px;
      text-align: center;
      font-size: 0.9rem;
      user-select: none;
    }

    /* Responsive spacing */
    @media (max-width: 640px) {
      nav {
        display: none; /* simplification for now on mobile */
      }
      header {
        justify-content: center;
      }
      .hero p {
        max-width: 90vw;
      }
      .features {
        margin: 48px 0;
        gap: 24px;
      }
      .btn-primary {
        padding: 12px 36px;
        font-size: 1rem;
      }
    }

  </style>
</head>
<body>
  <header>
    <div class="logo" tabindex="0">DiseñoGráfico</div>
    <nav role="navigation" aria-label="Menú principal">
      <a href="#features" tabindex="0">Servicios</a>
      <a href="#contacto" tabindex="0">Contacto</a>
    </nav>
  </header>

  <main>
    <section class="hero" role="banner" aria-label="Sección principal con llamada a la acción">
      <h1>Diseño Gráfico Dinámico y Profesional</h1>
      <p>Transforma tus ideas en imágenes que impactan. Creatividad en cada pixel y movimiento.</p>
      <button class="btn-primary" aria-label="Descubre más sobre nuestros servicios">Descubre Más</button>
    </section>

    <section id="features" class="container features" aria-label="Servicios destacados de diseño gráfico">
      <article class="feature-card" tabindex="0">
        <div class="feature-icon" aria-hidden="true">🎨</div>
        <h2 class="feature-title">Branding e Identidad Visual</h2>
        <p class="feature-desc">Crea una identidad memorable que refleje la esencia de tu marca y conecte con tus clientes.</p>
      </article>
      <article class="feature-card" tabindex="0">
        <div class="feature-icon" aria-hidden="true">🖌️</div>
        <h2 class="feature-title">Diseño Digital</h2>
        <p class="feature-desc">Diseños modernos para web, redes sociales y publicidad digital que capturan atención y convierten.</p>
      </article>
      <article class="feature-card" tabindex="0">
        <div class="feature-icon" aria-hidden="true">📄</div>
        <h2 class="feature-title">Material Impreso</h2>
        <p class="feature-desc">Flyers, carteles, catálogos y más, con calidad profesional y diseño que destaca en cualquier soporte.</p>
      </article>
    </section>

    <section id="contacto" class="container" style="margin-bottom: 72px;">
      <h2 style="text-align:center; margin-bottom: 32px; color: var(--blue-dark); font-weight: 800;">Contacto</h2>
      <form aria-label="Formulario de contacto para diseño gráfico" style="max-width: 600px; margin: 0 auto; display: flex; flex-direction: column; gap: 16px;">
        <label for="nombre" style="font-weight: 600; color: var(--blue-dark);">Nombre</label>
        <input id="nombre" name="nombre" type="text" placeholder="Tu nombre" required style="padding: 12px; border-radius: 8px; border: 2px solid var(--blue-light); font-size: 1rem;">

        <label for="email" style="font-weight: 600; color: var(--blue-dark);">Correo Electrónico</label>
        <input id="email" name="email" type="email" placeholder="tu@correo.com" required style="padding: 12px; border-radius: 8px; border: 2px solid var(--blue-light); font-size: 1rem;">

        <label for="mensaje" style="font-weight: 600; color: var(--blue-dark);">Mensaje</label>
        <textarea id="mensaje" name="mensaje" rows="4" placeholder="Cuéntanos sobre tu proyecto" required style="padding: 12px; border-radius: 8px; border: 2px solid var(--blue-light); font-size: 1rem; resize: vertical;"></textarea>

        <button type="submit" class="btn-primary" aria-label="Enviar mensaje de contacto">Enviar</button>
      </form>
    </section>
  </main>

  <footer>
    &copy; 2024 Diseño Gráfico Dinámico. Todos los derechos reservados.
  </footer>

  <script>
    // Button click dynamic feedback
    const btn = document.querySelector('.btn-primary');
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      btn.disabled = true;
      btn.textContent = 'Procesando...';

      // Simulate async operation
      setTimeout(() => {
        btn.textContent = 'Enviado!';
        btn.style.backgroundColor = '#0040ff';

        setTimeout(() => {
          btn.textContent = 'Descubre Más';
          btn.disabled = false;
          btn.style.backgroundColor = '';
        }, 2000);
      }, 1500);
    });

    // Simple focus visible polyfill for keyboard users (outline only on keyboard nav)
    function handleFirstTab(e) {
      if(e.key === 'Tab') {
        document.body.classList.add('user-is-tabbing');
        window.removeEventListener('keydown', handleFirstTab);
      }
    }
    window.addEventListener('keydown', handleFirstTab);
  </script>
</body>
</html>

